function checkRoute() {
    let tela = "/home.html"
    switch (window.location.pathname) {
        case "/alunos":
                
            break;
        case "/alunos/criar":
            
            break;
        
        default:
            break;
    }
}